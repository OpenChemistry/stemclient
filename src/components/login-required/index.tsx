import React from 'react';
import {Typography } from '@material-ui/core';

const LoginRequired = () => {
  return (
    <Typography >Authentication is required to access this page</Typography>
  )
}

export default LoginRequired;